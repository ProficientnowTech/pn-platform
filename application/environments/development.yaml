# Business Environment: Development
# Business application overrides

configPackage: core
environment: development

# Global defaults for all applications
global:
  domain: dev.local
  ingressClassName: nginx
  storageClass: standard

# ArgoCD app-of-apps configuration
argocd:
  syncWave: 0
  autoSync: true
  prune: true
  selfHeal: true
