apiVersion: apps/v1
kind: Deployment
metadata:
  name: reminders
  labels:
    app.kubernetes.io/name: reminders
    app.kubernetes.io/component: backend
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: reminders
  template:
    metadata:
      labels:
        app.kubernetes.io/name: reminders
        app.kubernetes.io/component: backend
    spec:
      containers:
        - name: reminders
          image: registry.pnats.cloud/pnats/reminders
          ports:
            - containerPort: 8090
              protocol: TCP
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              cpu: 500m
              memory: 512Mi
          readinessProbe:
            httpGet:
              path: /healthz
              port: 8090
            initialDelaySeconds: 10
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /healthz
              port: 8090
            initialDelaySeconds: 30
            periodSeconds: 30
---
apiVersion: v1
kind: Service
metadata:
  name: reminders
  labels:
    app.kubernetes.io/name: reminders
    app.kubernetes.io/component: backend
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: reminders
  ports:
    - port: 8090
      targetPort: 8090
      protocol: TCP
