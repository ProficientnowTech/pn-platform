apiVersion: apps/v1
kind: Deployment
metadata:
  name: audit-api
  labels:
    app.kubernetes.io/name: audit-api
    app.kubernetes.io/component: backend
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: audit-api
  template:
    metadata:
      labels:
        app.kubernetes.io/name: audit-api
        app.kubernetes.io/component: backend
    spec:
      containers:
        - name: audit-api
          image: registry.pnats.cloud/pnats/audit-api
          ports:
            - containerPort: 8080
              protocol: TCP
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              cpu: 500m
              memory: 512Mi
          readinessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 10
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 30
            periodSeconds: 30
---
apiVersion: v1
kind: Service
metadata:
  name: audit-api
  labels:
    app.kubernetes.io/name: audit-api
    app.kubernetes.io/component: backend
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: audit-api
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
