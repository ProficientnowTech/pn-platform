apiVersion: apps/v1
kind: Deployment
metadata:
  name: pi-mailsum
  labels:
    app.kubernetes.io/name: pi-mailsum
    app.kubernetes.io/component: ai
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: pi-mailsum
  template:
    metadata:
      labels:
        app.kubernetes.io/name: pi-mailsum
        app.kubernetes.io/component: ai
    spec:
      containers:
        - name: pi-mailsum
          image: registry.pnats.cloud/pnats/pi-mailsum
          ports:
            - containerPort: 8074
              protocol: TCP
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              cpu: 500m
              memory: 512Mi
          readinessProbe:
            httpGet:
              path: /health
              port: 8074
            initialDelaySeconds: 10
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: 8074
            initialDelaySeconds: 30
            periodSeconds: 30
---
apiVersion: v1
kind: Service
metadata:
  name: pi-mailsum
  labels:
    app.kubernetes.io/name: pi-mailsum
    app.kubernetes.io/component: ai
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: pi-mailsum
  ports:
    - port: 8074
      targetPort: 8074
      protocol: TCP
