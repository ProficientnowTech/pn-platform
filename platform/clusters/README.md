# platform/clusters — the cluster REGISTRY (data)

One file per cluster. Schema + rationale: docs/design/directory-structure-and-cluster-registry.md
The stack-orchestrator (P1) stamps registry x stacks into Applications.
OVH is intentionally absent (legacy prod being retired; not a future-fleet cluster).
