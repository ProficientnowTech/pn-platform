---
## Debezium Operator Configuration
## Manages DebeziumServer custom resources for Change Data Capture

debezium-operator:
  # Operator image (format: "repository:tag")
  image: "quay.io/debezium/operator:3.4.0"

  # Watch all namespaces by default
  watchNamespaces: "JOSDK_ALL_NAMESPACES"
