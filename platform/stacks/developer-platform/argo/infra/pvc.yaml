apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: ci-workspace
  namespace: argo
spec:
  storageClassName: ceph-filesystem
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 20Gi
